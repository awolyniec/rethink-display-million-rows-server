# Display million rows - server

Serves up a million rows, each differentiated by its row index.

Run with Node 12, npm 6, and yarn 1. Other versions not tested.


Run `yarn install` to install dependencies.